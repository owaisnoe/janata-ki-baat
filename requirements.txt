Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Limiter>=3.5
SQLAlchemy>=2.0
reportlab>=4.1
Pillow>=10.3
qrcode>=7.4
python-dotenv>=1.0
requests>=2.31
# MySQL driver for cPanel production (pure-python, installs clean on shared hosting).
# Local dev uses SQLite; this is only imported when DATABASE_URL points at MySQL.
PyMySQL>=1.1
